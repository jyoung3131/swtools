#Set this file to ~/.tmux.conf

set -g default-terminal "screen-256color"   # use 256 colors
set -g status-utf8 on                       # enable utf-8
set -g history-limit 100000                 # scrollback buffer n lines
set -g prefix `
unbind %
bind \ split-window -h
bind - split-window -v
